# Node / JS
node_modules/
dist/
build/
.vite/
.turbo/
*.tsbuildinfo

# PHP
vendor/
composer.lock

# Env files
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Test output
coverage/
playwright-report/
test-results/

# Editors
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Local caches written by the PHP toolchain.
.php-cs-fixer.cache
.phpunit.cache/

# Legacy WHM deploy artifact; monorepo layout does not use it.
public_html/

# Test plan lives locally only. Contains scenario notes we do not want to publish
# even in a private repo, because it will get pasted into GitHub issues and PRs
# and we want to keep it out of the git-tracked surface. CRITICAL: keep ignored.
docs/qa/test-plan.md
